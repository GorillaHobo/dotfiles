# -*- mode: snippet -*-
# name: React functional component
# key: rfc
# --

import { $1 } from 'react';

const ${2:`(f-base buffer-file-name)`} = (${3:props}) => {
  return(
    $0
  )
}

export default ${2:$(yas-text)};