# -*- mode: snippet -*-
# name: React component function
# uuid: rfc
# key: rfc
# condition: t
# --

import React from 'react';

const ${1:`(f-base buffer-file-name)`} = ($2) => {
  return (
    $0
  );
}

export default $1;