# -*- mode: snippet -*-
# name: styled react functional component
# key: rfs
# --
import styled from "@emotion/styled"

const Styled${1:`(f-base buffer-file-name)`} = styled.${2:div}\`
  $3
\`

const $1 = () => {
  return (
    <Styled$1>
      $0
    </Styled$1>
  )
}

export default $1